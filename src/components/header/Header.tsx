import { HeaderWrapper } from './Header.styles';

const Header: React.FC = () => {
  return (
    <HeaderWrapper>
      <h1 className="text">
        todos
      </h1>
    </HeaderWrapper>
  );
};

export default Header;
