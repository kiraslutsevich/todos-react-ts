import { TitleWrapper } from './Title.styles';

const Title: React.FC = () => {
  return (
    <TitleWrapper>
      <h1 className="text">
        todos
      </h1>
    </TitleWrapper>
  );
};

export default Title;
